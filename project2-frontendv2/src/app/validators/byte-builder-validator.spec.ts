import { ByteBuilderValidator } from './byte-builder-validator';

describe('ByteBuilderValidator', () => {
  it('should create an instance', () => {
    expect(new ByteBuilderValidator()).toBeTruthy();
  });
});
