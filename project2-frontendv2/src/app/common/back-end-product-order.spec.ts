import { BackEndProductOrder } from './back-end-product-order';

describe('BackEndProductOrder', () => {
  it('should create an instance', () => {
    expect(new BackEndProductOrder()).toBeTruthy();
  });
});
