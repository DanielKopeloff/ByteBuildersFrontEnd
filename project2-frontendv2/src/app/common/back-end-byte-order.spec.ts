import { BackEndByteOrder } from './back-end-byte-order';

describe('BackEndByteOrder', () => {
  // it('should create an instance', () => {
  //   expect(new BackEndByteOrder()).toBeTruthy();
  // });
});
